## Installation packages in virtual environments
```bash
# Creation enviroment (Ubuntu)
python3 -m venv venv
source venv/bin/activate

# Example
pip install flake8
flake8 --version

# Exit
deactivate
```